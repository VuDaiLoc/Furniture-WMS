<<<<<<< HEAD
<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luxury WMS</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            background-color: #f8f9fa;
        }

        .sidebar {
            width: 250px;
            background-color: #f0f0f0;
            height: 100vh;
            padding: 20px 0;
            position: fixed;
            left: 0;
            top: 0;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        }

        .sidebar h2 {
            text-align: center;
            font-size: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .sidebar img {
            width: 35px;
            height: 35px;
            border-radius: 50%;
            object-fit: cover;
        }
        .sidebar a {
            display: block;
            padding: 10px;
            text-decoration: none;
            color: black;
            border-radius: 5px;
            margin: 5px 10px;
            font-weight: bold;
        }

        .sidebar a:hover,
        .active {
            background-color: #007bff;
            color: white;
        }

         .dropdown {
            position: relative;
            margin: 5px 10px;
            padding: 10px;
            cursor: pointer;
            font-weight: bold;
            border-radius: 5px;
        }

        .dropdown:hover {
            background-color: #007bff;
            color: white;
        }

        .dropdown-menu {
            display: none;
            position: absolute;
            left: 100%;
            top: 0;
            background-color: white;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
            border-radius: 5px;
            min-width: 200px;
            z-index: 1000;
        }

        .dropdown:hover .dropdown-menu {
            display: block;
        }

        .dropdown-menu a {
            display: block;
            padding: 10px;
            text-decoration: none;
            color: black;
        }

        .dropdown-menu a:hover {
            background-color: #007bff;
            color: white;
        }

        .content {
            margin-left: 250px;
            width: calc(100% - 250px);
            padding: 20px;
        }

        .topbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            background-color: white;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            position: fixed;
            left: 250px;
            width: calc(100% - 250px);
            top: 0;
            z-index: 1000;
            height: 60px;
        }

        .search-bar {
            width: 50%;
            padding: 8px;
            border-radius: 20px;
            border: 1px solid #ccc;
        }

        .logout-button {
            padding: 8px 15px;
            border-radius: 20px;
            border: 1px solid #d9534f;
            background-color: white;
            color: #d9534f;
            cursor: pointer;
        }

        .logout-button:hover {
            background-color: #d9534f;
            color: white;
        }

        .dashboard-content {
            margin-top: 80px;
            margin-left: 0;
            padding: 20px;
        }

        .charts {
            display: flex;
            gap: 20px;
            width: 100%;
        }

        .chart-container {
            flex: 1;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 50%;
        }

         .order-section {
            width: 100%;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th, td {
            padding: 15px;
            text-align: center;
            border-bottom: 1px solid #ddd;
        }

        th {
            background-color: #007bff;
            color: white;
        }

        /* Footer */
        .footer {
            position: fixed;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 10px;
            background-color: #f8f9fa;
            text-align: left;
            font-size: 14px;
            color: #6c757d;
        }

    </style>
</head>

<body>
    <div class="sidebar">
         <h2>
            <img src="~/images/logo.png" alt="Luxury WMS Logo"> Luxury WMS
        </h2>
        <a href="#" class="active">Trang chủ</a>
        
         <div class="dropdown">
            Danh Mục ▼
            <div class="dropdown-menu">
                <a href="#">Phòng khách</a>
                <a href="#">Phòng ngủ</a>
                <a href="#">Phòng bếp</a>
                <a href="#">Phòng làm việc</a>
                <a href="#">Phòng vệ sinh</a>
            </div>
        </div>

        <a href="#"><i class="fas fa-box"></i> Quản lý sản phẩm</a>
    <a href="#"><i class="fas fa-shopping-cart"></i> Quản lý đơn hàng</a>
    <a href="#"><i class="fas fa-users"></i> Khách hàng</a>
    <a href="#"><i class="fas fa-warehouse"></i> Nhập kho</a>
    <a href="#"><i class="fas fa-truck-loading"></i> Xuất kho</a>
    <a href="#"><i class="fas fa-user-shield"></i> Quản lý người dùng</a>
    </div>

    <div class="content">
        <div class="topbar">
            <input type="text" class="search-bar" placeholder="Tìm kiếm...">
            <button class="logout-button">Đăng xuất</button>
        </div>

        <div class="dashboard-content">
            <div class="charts">
                <div class="chart-container">
                    <h3>Tổng doanh thu</h3>
                    <canvas id="barChart"></canvas>
                </div>
                <div class="chart-container">
                    <h3>Tổng sản phẩm</h3>
                    <canvas id="pieChart"></canvas>
                </div>
            </div>
        </div>
         <div class="order-section">
                <h3>Đơn hàng gần đây</h3>
                <table>
                    <tr>
                        <th>Tên</th>
                        <th>Email</th>
                        <th>Tổng tiền</th>
                        <th>Ngày</th>
                        <th>Chi tiết</th>
                    </tr>
                </table>
            </div>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var ctx1 = document.getElementById('barChart').getContext('2d');
            new Chart(ctx1, {
                type: 'bar',
                data: {
                    labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5'],
                    datasets: [{
                        label: 'Doanh thu',
                        data: [12000, 19000, 3000, 5000, 20000],
                        backgroundColor: 'rgba(54, 162, 235, 0.6)'
                    }]
                }
            });

            var ctx2 = document.getElementById('pieChart').getContext('2d');
            new Chart(ctx2, {
                type: 'pie',
                data: {
                    labels: ['Sofa', 'Bàn', 'Tủ', 'Ghế'],
                    datasets: [{
                        data: [30, 20, 25, 25],
                        backgroundColor: ['red', 'blue', 'green', 'yellow']
                    }]
                }
            });
        });
    </script>

    <div class="footer">
        &copy; 2025 - Luxury WMS
    </div>
</body>

</html>
=======
@{
    ViewData["Title"] = "Dashboard";
    Layout = "~/Views/Shared/_Layout.cshtml"; // Sử dụng layout chung
}

<div class="dashboard-container">
    <!-- Sidebar -->
    <div class="sidebar">
        <h2 class="logo-container">
            <img src="~/images/logo.png" alt="Luxury WMS Logo" class="logo">
            <span>Luxury WMS</span>
        </h2>
        <a href="/Home/Index" class="active">Trang chủ</a>

        <!-- Dropdown Danh Mục -->
        <div class="dropdown">
            <a href="#" class="dropdown-toggle">Danh Mục ▼</a>
            <div class="dropdown-menu">
                <a href="/Category/PhongKhach">Phòng khách</a>
                <a href="/Category/PhongNgu">Phòng ngủ</a>
                <a href="/Category/PhongBep">Phòng bếp</a>
                <a href="/Category/PhongLamViec">Phòng làm việc</a>
                <a href="/Category/PhongVeSinh">Phòng vệ sinh</a>
            </div>
        </div>

        <a href="/Product/Index"><i class="fas fa-box"></i> Quản lý sản phẩm</a>
        <a href="/Order/Index"><i class="fas fa-shopping-cart"></i> Quản lý đơn hàng</a>
        <a href="/Customer/Index"><i class="fas fa-users"></i> Khách hàng</a>
        <a href="/Warehouse/NhapKho"><i class="fas fa-warehouse"></i> Nhập kho</a>
        <a href="/Warehouse/XuatKho"><i class="fas fa-truck-loading"></i> Xuất kho</a>

        @if (Context.Session.GetString("Role") == "Admin")
        {
            <a href="/User/Index"><i class="fas fa-user-shield"></i> Quản lý người dùng</a>
        }
    </div>

    <!-- Nội dung chính -->
    <div class="content">
        <!-- Topbar -->
        <div class="topbar">
            <input type="text" class="search-bar" placeholder="Tìm kiếm...">
            <a href="/Account/Logout" class="btn btn-danger logout-button">Đăng xuất</a>
        </div>

        <!-- Biểu đồ -->
        <div class="charts">
            <div class="chart-container">
                <h3>Tổng doanh thu</h3>
                <canvas id="barChart"></canvas>
            </div>
            <div class="chart-container">
                <h3>Tổng sản phẩm</h3>
                <canvas id="pieChart"></canvas>
            </div>
        </div>

        <!-- Đơn hàng gần đây -->
        <div class="order-section">
            <h3>Đơn hàng gần đây</h3>
            <table>
                <tr>
                    <th>Tên</th>
                    <th>Email</th>
                    <th>Tổng tiền</th>
                    <th>Ngày</th>
                    <th>Chi tiết</th>
                </tr>
            </table>
        </div>
    </div>
</div>

<!-- Footer -->
<footer class="main-footer">
    <div class="footer-container">
        <div class="footer-bottom">
            <p>Copyright © 2025 Luxury WMS. All Rights Reserved.</p>
            <div class="social-icons">
                <a href="#"><img src="~/lib/images/Logo/Facebook.jpg" alt="Facebook"></a>
                <a href="#"><img src="~/lib/images/Logo/Messenger.jpg" alt="Messenger"></a>
                <a href="#"><img src="~/lib/images/Logo/Zalo.png" alt="Zalo"></a>
            </div>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    document.addEventListener("DOMContentLoaded", function () {
        var ctx1 = document.getElementById('barChart').getContext('2d');
        new Chart(ctx1, {
            type: 'bar',
            data: {
                labels: ['Tháng 1', 'Tháng 2', 'Tháng 3', 'Tháng 4', 'Tháng 5'],
                datasets: [{
                    label: 'Doanh thu',
                    data: [12000, 19000, 3000, 5000, 20000],
                    backgroundColor: 'rgba(54, 162, 235, 0.6)'
                }]
            }
        });

        var ctx2 = document.getElementById('pieChart').getContext('2d');
        new Chart(ctx2, {
            type: 'doughnut',
            data: {
                labels: ['Sofa', 'Bàn', 'Tủ', 'Ghế'],
                datasets: [{
                    data: [25, 25, 25, 25],
                    backgroundColor: ['red', 'blue', 'green', 'yellow']
                }]
            }
        });

        // Hiệu ứng dropdown menu
        document.querySelector(".dropdown-toggle").addEventListener("click", function (event) {
            event.preventDefault();
            var menu = document.querySelector(".dropdown-menu");
            menu.classList.toggle("show");
        });

        // Ẩn menu khi click ra ngoài
        document.addEventListener("click", function (event) {
            var dropdown = document.querySelector(".dropdown");
            if (!dropdown.contains(event.target)) {
                document.querySelector(".dropdown-menu").classList.remove("show");
            }
        });
    });
</script>
>>>>>>> b9741e2 (Phuc Update)
